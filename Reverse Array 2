class Solution {
    public void reverseArray(int arr[]) {
        int l = arr.length;
        int start=0;
        int end=l-1;

   while(start<end){
       int temp;
       temp=arr[start];
       arr[start]=arr[end];
       arr[end]=temp;
       
       start++;
       end--;
   }
   }
}
